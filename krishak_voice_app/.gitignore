node_modules/
.next/
out/
build/
.env
.env.local
.env*.local
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel

# Local source audio (lives outside the repo)
*.mp3
*.wav
*.m4a
# …except the pre-rendered first-message audio: it's a production asset
# (eliminates ~500ms TTS gap on turn 0), not voice-clone training material.
!public/first-message.mp3
*Zone.Identifier
